#!/usr/bin/env bash
# Cross-platform launcher (macOS / Linux / Git Bash)
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
export PYTHONPATH="${ROOT}/src${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 -m talaria_cli "$@"
