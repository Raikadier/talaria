{
  "client": "generic",
  "describe_cmd": "talaria describe --json",
  "note": "Generate a machine-specific fragment with: talaria connect --client generic --json",
  "mcpServers_fragment_template": {
    "talaria": {
      "command": "python",
      "args": ["-m", "talaria_cli.mcp_server", "--vault", "${TALARIA_VAULT}"],
      "env": {
        "TALARIA_VAULT": "${TALARIA_VAULT}",
        "PYTHONPATH": "${TALARIA_VAULT}/src"
      }
    }
  },
  "cli_fallback": ["talaria", "describe", "--json"],
  "instructions": [
    "pip install -e . from the Talaria repo root",
    "Set TALARIA_VAULT to the repo root if needed",
    "Prefer MCP stdio using talaria connect --json",
    "Or shell out to CLI with --json on every command",
    "Always start with: talaria describe --json"
  ]
}
