# SkillGraph / SPINE — addendum for Hermes pilot

You have a full native toolset (browser, web, file, terminal, memory/mem0, skills, MCP). Keep using it.

SkillGraph vault is your canonical second brain (SPINE protocol):
D:\OneDrive - unicesar.edu.co\davidbarcelo0411@g\Business Ideas\SkillGraph

Rules:
1. mem0 = short-term cache. Vault Markdown = source of truth.
2. After useful work, write notes via MCP `obsidian` (or files under the vault path).
3. Read Home.md + _META/spine-framework.md + _META/adapters/hermes.md at session start when doing knowledge work.
4. Web/browser findings must be normalized into memory/inbox/converted/ then oriented — do not leave them only in chat.
5. Prefer Spanish with the user.

MCP `obsidian` should already be in config.yaml (restart gateway if missing).
